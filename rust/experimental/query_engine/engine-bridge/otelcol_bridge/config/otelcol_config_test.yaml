receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317  # Standard OTLP gRPC port; adjust if needed
processors:
  otelcol_bridge:
    
exporters:
  debug:

service:
  pipelines:
    logs:
      receivers: [otlp]
      processors: [otelcol_bridge]
      exporters: [debug]